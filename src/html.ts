// import ReactDOMServer from "react-dom/server";

// export async function render(filePath: string, ctx: any) {
//   const html = ReactDOMServer.renderToString(
//     (await import(filePath + "?n=" + Date.now())).default(ctx)
//   );
//   return html;
// }
